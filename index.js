let currentUser = "Grace Hopper";

let welcomeMessage = "Welcome to Flatbook, " + currentUser + "!";

let excitedWelcomeMessage =
  "WELCOME TO FLATBOOK, " + currentUser.toUpperCase() + "!";

let shortGreeting = "Welcome, " + currentUser[0].toUpperCase() + "!";
